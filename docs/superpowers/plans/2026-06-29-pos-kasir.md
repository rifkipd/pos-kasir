# POS / Kasir Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bangun aplikasi web POS satu-toko dengan manajemen produk, transaksi kasir (hitung total & kembalian otomatis), cetak struk thermal, dan laporan penjualan dasar.

**Architecture:** Next.js (App Router) full-stack — UI React di client, logika di Route Handlers. Logika bisnis murni ditaruh di `lib/` sebagai fungsi yang menerima Prisma client sebagai argumen sehingga mudah diuji. Database SQLite via Prisma. UI memakai Tailwind + shadcn/ui dengan arah visual `minimalist-ui`.

**Tech Stack:** TypeScript, Next.js 15 (App Router), React 19, Tailwind CSS, shadcn/ui, Prisma, SQLite, Recharts, Vitest (unit test), bcryptjs (hash password), iron-session (sesi login).

## Global Constraints

- Bahasa kode & komentar: campuran boleh, tapi identifier dalam bahasa Inggris.
- Mata uang: Rupiah, disimpan sebagai **integer** (rupiah penuh, tanpa desimal) untuk menghindari error pembulatan float.
- Semua mutasi yang menyentuh stok + transaksi WAJIB dalam satu `prisma.$transaction`.
- `priceAtSale` disalin dari harga produk saat transaksi dibuat; laporan memakai nilai ini, bukan harga produk terkini.
- TDD: tulis test gagal dulu, lalu implementasi minimal. Commit tiap task.
- Node 20+. Package manager: npm.
- Test runner: Vitest. Jalankan via `npm test`.

---

## File Structure

```
POS/
  prisma/
    schema.prisma          # model Product, Transaction, TransactionItem, User
    seed.ts                # data contoh untuk dev
  src/
    lib/
      prisma.ts            # singleton Prisma client
      money.ts             # format & parse rupiah (pure)
      cart.ts              # hitung total & kembalian (pure)
      products.ts          # service CRUD produk (terima prisma)
      transactions.ts      # service buat transaksi + kurangi stok (terima prisma)
      reports.ts           # service query laporan (terima prisma)
      auth.ts              # hash/verify password, helper sesi
    app/
      layout.tsx           # root layout + font + base minimalist-ui
      globals.css          # Tailwind + token warna minimalist-ui
      page.tsx             # redirect ke /kasir (atau /login)
      login/page.tsx
      kasir/page.tsx
      produk/page.tsx
      laporan/page.tsx
      api/
        login/route.ts
        logout/route.ts
        products/route.ts          # GET list, POST create
        products/[id]/route.ts     # PATCH update, DELETE
        transactions/route.ts      # GET list, POST create
        transactions/[id]/route.ts # GET detail
        reports/summary/route.ts   # GET ringkasan + rentang tanggal
      components/
        AppNav.tsx
        ProductForm.tsx
        Cart.tsx
        Receipt.tsx        # struk 80mm untuk print
  tests/
    money.test.ts
    cart.test.ts
    products.test.ts
    transactions.test.ts
    reports.test.ts
    auth.test.ts
  vitest.config.ts
  .env                     # DATABASE_URL, SESSION_PASSWORD
```

Logika bisnis (`cart`, `products`, `transactions`, `reports`) dipisah dari Route Handlers supaya bisa diuji tanpa HTTP. Route Handlers tipis: parse request → panggil service → balas JSON.

---

### Task 1: Scaffold proyek Next.js + Tailwind + Vitest

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `tailwind.config.ts`, `postcss.config.mjs`, `vitest.config.ts`, `.gitignore`, `.env`
- Create: `src/app/layout.tsx`, `src/app/globals.css`, `src/app/page.tsx`
- Test: `tests/smoke.test.ts`

**Interfaces:**
- Produces: skrip npm `dev`, `build`, `test`; alias import `@/*` → `src/*`.

- [ ] **Step 1: Scaffold Next.js**

Run:
```bash
npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*" --no-eslint --use-npm --yes
```
Expected: proyek terbentuk di folder saat ini (jawab "yes" jika diminta menimpa folder non-kosong — file docs/ aman).

- [ ] **Step 2: Pasang dependensi runtime & test**

Run:
```bash
npm install @prisma/client bcryptjs iron-session recharts
npm install -D prisma vitest @vitejs/plugin-react vite-tsconfig-paths @types/bcryptjs
```
Expected: terpasang tanpa error.

- [ ] **Step 3: Buat `vitest.config.ts`**

```ts
import { defineConfig } from "vitest/config";
import tsconfigPaths from "vite-tsconfig-paths";

export default defineConfig({
  plugins: [tsconfigPaths()],
  test: {
    environment: "node",
    include: ["tests/**/*.test.ts"],
  },
});
```

- [ ] **Step 4: Tambah skrip test ke `package.json`**

Tambahkan ke bagian `"scripts"`:
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 5: Buat `.env`**

```
DATABASE_URL="file:./dev.db"
SESSION_PASSWORD="ubah-ini-minimal-32-karakter-acak-panjang"
```

- [ ] **Step 6: Tulis smoke test gagal**

`tests/smoke.test.ts`:
```ts
import { expect, test } from "vitest";

test("vitest jalan", () => {
  expect(1 + 1).toBe(2);
});
```

- [ ] **Step 7: Jalankan test**

Run: `npm test`
Expected: PASS (1 test).

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "chore: scaffold next.js + tailwind + vitest"
```

---

### Task 2: Skema Prisma + SQLite + seed

**Files:**
- Create: `prisma/schema.prisma`, `prisma/seed.ts`
- Create: `src/lib/prisma.ts`
- Test: (diverifikasi lewat task lain; task ini hanya scaffolding DB)

**Interfaces:**
- Produces: model `Product { id, name, sku, price, stock, category, isActive }`, `Transaction { id, invoiceNo, createdAt, totalAmount, paidAmount, changeAmount, paymentMethod, items }`, `TransactionItem { id, transactionId, productId, quantity, priceAtSale, subtotal }`, `User { id, username, passwordHash }`.
- Produces: `import { prisma } from "@/lib/prisma"`.

- [ ] **Step 1: Tulis `prisma/schema.prisma`**

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Product {
  id        Int      @id @default(autoincrement())
  name      String
  sku       String?  @unique
  price     Int
  stock     Int      @default(0)
  category  String?
  isActive  Boolean  @default(true)
  items     TransactionItem[]
  createdAt DateTime @default(now())
}

model Transaction {
  id            Int      @id @default(autoincrement())
  invoiceNo     String   @unique
  createdAt     DateTime @default(now())
  totalAmount   Int
  paidAmount    Int
  changeAmount  Int
  paymentMethod String   @default("cash")
  items         TransactionItem[]
}

model TransactionItem {
  id            Int         @id @default(autoincrement())
  transactionId Int
  productId     Int
  quantity      Int
  priceAtSale   Int
  subtotal      Int
  transaction   Transaction @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  product       Product     @relation(fields: [productId], references: [id])
}

model User {
  id           Int    @id @default(autoincrement())
  username     String @unique
  passwordHash String
}
```

- [ ] **Step 2: Buat `src/lib/prisma.ts`**

```ts
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as { prisma?: PrismaClient };

export const prisma = globalForPrisma.prisma ?? new PrismaClient();

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;
```

- [ ] **Step 3: Jalankan migrasi awal**

Run: `npx prisma migrate dev --name init`
Expected: file `dev.db` terbuat, folder `prisma/migrations/` muncul, client ter-generate.

- [ ] **Step 4: Tulis `prisma/seed.ts`**

```ts
import { PrismaClient } from "@prisma/client";
import bcrypt from "bcryptjs";

const prisma = new PrismaClient();

async function main() {
  await prisma.user.upsert({
    where: { username: "admin" },
    update: {},
    create: { username: "admin", passwordHash: bcrypt.hashSync("admin123", 10) },
  });
  await prisma.product.createMany({
    data: [
      { name: "Kopi", sku: "KP01", price: 10000, stock: 100, category: "Minuman" },
      { name: "Teh", sku: "TH01", price: 8000, stock: 100, category: "Minuman" },
      { name: "Roti", sku: "RT01", price: 8000, stock: 50, category: "Makanan" },
    ],
  });
}

main().finally(() => prisma.$disconnect());
```

- [ ] **Step 5: Daftarkan seed di `package.json`**

Tambahkan blok top-level:
```json
"prisma": { "seed": "npx tsx prisma/seed.ts" }
```
Lalu pasang tsx: `npm install -D tsx`

- [ ] **Step 6: Jalankan seed**

Run: `npx prisma db seed`
Expected: user admin + 3 produk masuk tanpa error.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: prisma schema, sqlite migration, seed"
```

---

### Task 3: Logika uang (format & parse rupiah) — pure, TDD

**Files:**
- Create: `src/lib/money.ts`
- Test: `tests/money.test.ts`

**Interfaces:**
- Produces: `formatRupiah(n: number): string` → `"Rp 10.000"`; `parseRupiah(s: string): number` (buang non-digit → integer).

- [ ] **Step 1: Tulis test gagal**

`tests/money.test.ts`:
```ts
import { expect, test } from "vitest";
import { formatRupiah, parseRupiah } from "@/lib/money";

test("formatRupiah memberi pemisah ribuan", () => {
  expect(formatRupiah(10000)).toBe("Rp 10.000");
  expect(formatRupiah(0)).toBe("Rp 0");
  expect(formatRupiah(1500000)).toBe("Rp 1.500.000");
});

test("parseRupiah membuang karakter non-digit", () => {
  expect(parseRupiah("Rp 10.000")).toBe(10000);
  expect(parseRupiah("50000")).toBe(50000);
  expect(parseRupiah("")).toBe(0);
});
```

- [ ] **Step 2: Jalankan test**

Run: `npx vitest run tests/money.test.ts`
Expected: FAIL ("Cannot find module '@/lib/money'").

- [ ] **Step 3: Implementasi**

`src/lib/money.ts`:
```ts
export function formatRupiah(n: number): string {
  return "Rp " + Math.round(n).toLocaleString("id-ID");
}

export function parseRupiah(s: string): number {
  const digits = s.replace(/[^0-9]/g, "");
  return digits === "" ? 0 : parseInt(digits, 10);
}
```

- [ ] **Step 4: Jalankan test**

Run: `npx vitest run tests/money.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/money.ts tests/money.test.ts
git commit -m "feat: money format/parse helpers"
```

---

### Task 4: Logika keranjang (total & kembalian) — pure, TDD

**Files:**
- Create: `src/lib/cart.ts`
- Test: `tests/cart.test.ts`

**Interfaces:**
- Consumes: tidak ada.
- Produces:
  - `type CartLine = { productId: number; name: string; price: number; quantity: number }`
  - `calcSubtotal(line: CartLine): number`
  - `calcTotal(lines: CartLine[]): number`
  - `calcChange(total: number, paid: number): number` (kembalian; bila `paid < total` lempar `Error("Uang bayar kurang dari total")`)

- [ ] **Step 1: Tulis test gagal**

`tests/cart.test.ts`:
```ts
import { expect, test } from "vitest";
import { calcSubtotal, calcTotal, calcChange, type CartLine } from "@/lib/cart";

const lines: CartLine[] = [
  { productId: 1, name: "Kopi", price: 10000, quantity: 2 },
  { productId: 3, name: "Roti", price: 8000, quantity: 1 },
];

test("calcSubtotal = harga * qty", () => {
  expect(calcSubtotal(lines[0])).toBe(20000);
});

test("calcTotal menjumlah semua subtotal", () => {
  expect(calcTotal(lines)).toBe(28000);
  expect(calcTotal([])).toBe(0);
});

test("calcChange menghitung kembalian", () => {
  expect(calcChange(28000, 50000)).toBe(22000);
  expect(calcChange(28000, 28000)).toBe(0);
});

test("calcChange menolak uang kurang", () => {
  expect(() => calcChange(28000, 20000)).toThrow("Uang bayar kurang dari total");
});
```

- [ ] **Step 2: Jalankan test**

Run: `npx vitest run tests/cart.test.ts`
Expected: FAIL ("Cannot find module '@/lib/cart'").

- [ ] **Step 3: Implementasi**

`src/lib/cart.ts`:
```ts
export type CartLine = {
  productId: number;
  name: string;
  price: number;
  quantity: number;
};

export function calcSubtotal(line: CartLine): number {
  return line.price * line.quantity;
}

export function calcTotal(lines: CartLine[]): number {
  return lines.reduce((sum, l) => sum + calcSubtotal(l), 0);
}

export function calcChange(total: number, paid: number): number {
  if (paid < total) throw new Error("Uang bayar kurang dari total");
  return paid - total;
}
```

- [ ] **Step 4: Jalankan test**

Run: `npx vitest run tests/cart.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/cart.ts tests/cart.test.ts
git commit -m "feat: cart total & change calculation"
```

---

### Task 5: Service produk (CRUD) — TDD terhadap SQLite

**Files:**
- Create: `src/lib/products.ts`
- Test: `tests/products.test.ts`

**Interfaces:**
- Consumes: `prisma` dari `@/lib/prisma`, model `Product`.
- Produces (semua menerima `db: PrismaClient` sebagai argumen pertama agar bisa diuji):
  - `listProducts(db, opts?: { activeOnly?: boolean }): Promise<Product[]>`
  - `createProduct(db, input: { name: string; price: number; stock?: number; sku?: string; category?: string }): Promise<Product>`
  - `updateProduct(db, id: number, input: Partial<{ name; price; stock; sku; category; isActive }>): Promise<Product>`
  - `deleteProduct(db, id: number): Promise<void>` (soft delete: set `isActive=false`)
  - Validasi: `createProduct` melempar `Error("Harga tidak boleh negatif")` bila price < 0, dan `Error("Nama wajib diisi")` bila name kosong.

- [ ] **Step 1: Buat helper DB test**

`tests/helpers/db.ts`:
```ts
import { PrismaClient } from "@prisma/client";
import { execSync } from "node:child_process";
import { existsSync, rmSync } from "node:fs";

export function freshDb(file: string): PrismaClient {
  const url = `file:./${file}`;
  if (existsSync(file)) rmSync(file);
  execSync(`npx prisma db push --skip-generate`, {
    env: { ...process.env, DATABASE_URL: url },
    stdio: "ignore",
  });
  return new PrismaClient({ datasources: { db: { url } } });
}
```

- [ ] **Step 2: Tulis test gagal**

`tests/products.test.ts`:
```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import type { PrismaClient } from "@prisma/client";
import { freshDb } from "./helpers/db";
import { createProduct, listProducts, updateProduct, deleteProduct } from "@/lib/products";

let db: PrismaClient;
beforeAll(() => { db = freshDb("test-products.db"); });
afterAll(async () => { await db.$disconnect(); });

test("createProduct lalu listProducts", async () => {
  const p = await createProduct(db, { name: "Kopi", price: 10000, stock: 5 });
  expect(p.id).toBeGreaterThan(0);
  const all = await listProducts(db);
  expect(all.find((x) => x.id === p.id)?.name).toBe("Kopi");
});

test("createProduct menolak harga negatif", async () => {
  await expect(createProduct(db, { name: "X", price: -1 })).rejects.toThrow("Harga tidak boleh negatif");
});

test("createProduct menolak nama kosong", async () => {
  await expect(createProduct(db, { name: "", price: 1 })).rejects.toThrow("Nama wajib diisi");
});

test("updateProduct mengubah harga", async () => {
  const p = await createProduct(db, { name: "Teh", price: 8000 });
  const u = await updateProduct(db, p.id, { price: 9000 });
  expect(u.price).toBe(9000);
});

test("deleteProduct = soft delete (activeOnly menyembunyikan)", async () => {
  const p = await createProduct(db, { name: "Roti", price: 8000 });
  await deleteProduct(db, p.id);
  const active = await listProducts(db, { activeOnly: true });
  expect(active.find((x) => x.id === p.id)).toBeUndefined();
});
```

- [ ] **Step 3: Jalankan test**

Run: `npx vitest run tests/products.test.ts`
Expected: FAIL ("Cannot find module '@/lib/products'").

- [ ] **Step 4: Implementasi**

`src/lib/products.ts`:
```ts
import type { PrismaClient, Product } from "@prisma/client";

type CreateInput = { name: string; price: number; stock?: number; sku?: string; category?: string };
type UpdateInput = Partial<{ name: string; price: number; stock: number; sku: string; category: string; isActive: boolean }>;

function validate(input: { name?: string; price?: number }) {
  if (input.name !== undefined && input.name.trim() === "") throw new Error("Nama wajib diisi");
  if (input.price !== undefined && input.price < 0) throw new Error("Harga tidak boleh negatif");
}

export function listProducts(db: PrismaClient, opts?: { activeOnly?: boolean }): Promise<Product[]> {
  return db.product.findMany({
    where: opts?.activeOnly ? { isActive: true } : undefined,
    orderBy: { name: "asc" },
  });
}

export function createProduct(db: PrismaClient, input: CreateInput): Promise<Product> {
  validate(input);
  return db.product.create({
    data: {
      name: input.name.trim(),
      price: input.price,
      stock: input.stock ?? 0,
      sku: input.sku || null,
      category: input.category || null,
    },
  });
}

export function updateProduct(db: PrismaClient, id: number, input: UpdateInput): Promise<Product> {
  validate(input);
  return db.product.update({ where: { id }, data: input });
}

export async function deleteProduct(db: PrismaClient, id: number): Promise<void> {
  await db.product.update({ where: { id }, data: { isActive: false } });
}
```

- [ ] **Step 5: Jalankan test**

Run: `npx vitest run tests/products.test.ts`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/lib/products.ts tests/products.test.ts tests/helpers/db.ts
git commit -m "feat: product CRUD service with validation"
```

---

### Task 6: Service transaksi (buat transaksi + kurangi stok atomik) — TDD

**Files:**
- Create: `src/lib/transactions.ts`
- Test: `tests/transactions.test.ts`

**Interfaces:**
- Consumes: `prisma`, model `Transaction`, `Product`; `CartLine` dari `@/lib/cart`.
- Produces:
  - `createTransaction(db, input: { lines: { productId: number; quantity: number }[]; paidAmount: number; paymentMethod?: string }): Promise<TransactionWithItems>`
  - Perilaku: harga & subtotal diambil dari DB (server-trusted, bukan dari client), total dihitung server, `changeAmount = paid - total`. Stok tiap produk dikurangi. Semua dalam satu `$transaction`.
  - Error: `Error("Keranjang kosong")` bila lines kosong; `Error("Stok ... tidak cukup")` bila qty > stok; `Error("Uang bayar kurang dari total")` bila paid < total. Saat error, TIDAK ada perubahan tersimpan.
  - `invoiceNo` di-generate: `INV-<timestamp ms>`.
  - `TransactionWithItems = Transaction & { items: TransactionItem[] }`.

- [ ] **Step 1: Tulis test gagal**

`tests/transactions.test.ts`:
```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import type { PrismaClient } from "@prisma/client";
import { freshDb } from "./helpers/db";
import { createProduct } from "@/lib/products";
import { createTransaction } from "@/lib/transactions";

let db: PrismaClient;
let kopiId: number;
beforeAll(async () => {
  db = freshDb("test-tx.db");
  const kopi = await createProduct(db, { name: "Kopi", price: 10000, stock: 5 });
  kopiId = kopi.id;
});
afterAll(async () => { await db.$disconnect(); });

test("createTransaction menghitung total & kembalian, mengurangi stok", async () => {
  const tx = await createTransaction(db, {
    lines: [{ productId: kopiId, quantity: 2 }],
    paidAmount: 50000,
  });
  expect(tx.totalAmount).toBe(20000);
  expect(tx.changeAmount).toBe(30000);
  expect(tx.items).toHaveLength(1);
  expect(tx.items[0].priceAtSale).toBe(10000);
  const kopi = await db.product.findUnique({ where: { id: kopiId } });
  expect(kopi?.stock).toBe(3);
});

test("menolak keranjang kosong", async () => {
  await expect(createTransaction(db, { lines: [], paidAmount: 0 })).rejects.toThrow("Keranjang kosong");
});

test("menolak uang kurang tanpa mengubah stok", async () => {
  const before = (await db.product.findUnique({ where: { id: kopiId } }))!.stock;
  await expect(
    createTransaction(db, { lines: [{ productId: kopiId, quantity: 1 }], paidAmount: 1 }),
  ).rejects.toThrow("Uang bayar kurang dari total");
  const after = (await db.product.findUnique({ where: { id: kopiId } }))!.stock;
  expect(after).toBe(before);
});

test("menolak stok tidak cukup", async () => {
  await expect(
    createTransaction(db, { lines: [{ productId: kopiId, quantity: 999 }], paidAmount: 99999999 }),
  ).rejects.toThrow("Stok");
});
```

- [ ] **Step 2: Jalankan test**

Run: `npx vitest run tests/transactions.test.ts`
Expected: FAIL ("Cannot find module '@/lib/transactions'").

- [ ] **Step 3: Implementasi**

`src/lib/transactions.ts`:
```ts
import type { PrismaClient, Transaction, TransactionItem } from "@prisma/client";

export type TransactionWithItems = Transaction & { items: TransactionItem[] };

type Input = {
  lines: { productId: number; quantity: number }[];
  paidAmount: number;
  paymentMethod?: string;
};

export function createTransaction(db: PrismaClient, input: Input): Promise<TransactionWithItems> {
  if (input.lines.length === 0) throw new Error("Keranjang kosong");

  return db.$transaction(async (tx) => {
    let total = 0;
    const itemData: { productId: number; quantity: number; priceAtSale: number; subtotal: number }[] = [];

    for (const line of input.lines) {
      const product = await tx.product.findUnique({ where: { id: line.productId } });
      if (!product) throw new Error(`Produk ${line.productId} tidak ditemukan`);
      if (line.quantity <= 0) throw new Error("Jumlah harus lebih dari 0");
      if (product.stock < line.quantity) throw new Error(`Stok ${product.name} tidak cukup`);
      const subtotal = product.price * line.quantity;
      total += subtotal;
      itemData.push({ productId: product.id, quantity: line.quantity, priceAtSale: product.price, subtotal });
    }

    if (input.paidAmount < total) throw new Error("Uang bayar kurang dari total");

    for (const item of itemData) {
      await tx.product.update({
        where: { id: item.productId },
        data: { stock: { decrement: item.quantity } },
      });
    }

    return tx.transaction.create({
      data: {
        invoiceNo: `INV-${Date.now()}`,
        totalAmount: total,
        paidAmount: input.paidAmount,
        changeAmount: input.paidAmount - total,
        paymentMethod: input.paymentMethod ?? "cash",
        items: { create: itemData },
      },
      include: { items: true },
    });
  });
}
```

- [ ] **Step 4: Jalankan test**

Run: `npx vitest run tests/transactions.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/transactions.ts tests/transactions.test.ts
git commit -m "feat: atomic transaction service with stock decrement"
```

---

### Task 7: Service laporan — TDD

**Files:**
- Create: `src/lib/reports.ts`
- Test: `tests/reports.test.ts`

**Interfaces:**
- Consumes: `prisma`, `createTransaction`.
- Produces:
  - `getSummary(db, range?: { from?: Date; to?: Date }): Promise<{ totalSales: number; transactionCount: number }>`
  - `getDailySales(db, range?: { from?: Date; to?: Date }): Promise<{ date: string; total: number }[]>` (dikelompokkan per tanggal `YYYY-MM-DD`, urut menaik)
  - `listTransactions(db, range?): Promise<TransactionWithItems[]>` (terbaru dulu)

- [ ] **Step 1: Tulis test gagal**

`tests/reports.test.ts`:
```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import type { PrismaClient } from "@prisma/client";
import { freshDb } from "./helpers/db";
import { createProduct } from "@/lib/products";
import { createTransaction } from "@/lib/transactions";
import { getSummary, getDailySales, listTransactions } from "@/lib/reports";

let db: PrismaClient;
beforeAll(async () => {
  db = freshDb("test-reports.db");
  const kopi = await createProduct(db, { name: "Kopi", price: 10000, stock: 100 });
  await createTransaction(db, { lines: [{ productId: kopi.id, quantity: 2 }], paidAmount: 20000 });
  await createTransaction(db, { lines: [{ productId: kopi.id, quantity: 1 }], paidAmount: 10000 });
});
afterAll(async () => { await db.$disconnect(); });

test("getSummary menjumlah penjualan & menghitung transaksi", async () => {
  const s = await getSummary(db);
  expect(s.totalSales).toBe(30000);
  expect(s.transactionCount).toBe(2);
});

test("getDailySales mengelompokkan per tanggal", async () => {
  const rows = await getDailySales(db);
  expect(rows.length).toBeGreaterThanOrEqual(1);
  const sum = rows.reduce((a, r) => a + r.total, 0);
  expect(sum).toBe(30000);
});

test("listTransactions mengembalikan terbaru dulu dengan items", async () => {
  const list = await listTransactions(db);
  expect(list).toHaveLength(2);
  expect(list[0].items.length).toBeGreaterThan(0);
});
```

- [ ] **Step 2: Jalankan test**

Run: `npx vitest run tests/reports.test.ts`
Expected: FAIL ("Cannot find module '@/lib/reports'").

- [ ] **Step 3: Implementasi**

`src/lib/reports.ts`:
```ts
import type { PrismaClient } from "@prisma/client";
import type { TransactionWithItems } from "@/lib/transactions";

type Range = { from?: Date; to?: Date };

function where(range?: Range) {
  if (!range?.from && !range?.to) return undefined;
  return { createdAt: { gte: range?.from, lte: range?.to } };
}

export async function getSummary(db: PrismaClient, range?: Range) {
  const rows = await db.transaction.findMany({ where: where(range), select: { totalAmount: true } });
  return {
    totalSales: rows.reduce((a, r) => a + r.totalAmount, 0),
    transactionCount: rows.length,
  };
}

export async function getDailySales(db: PrismaClient, range?: Range) {
  const rows = await db.transaction.findMany({
    where: where(range),
    select: { createdAt: true, totalAmount: true },
  });
  const byDay = new Map<string, number>();
  for (const r of rows) {
    const key = r.createdAt.toISOString().slice(0, 10);
    byDay.set(key, (byDay.get(key) ?? 0) + r.totalAmount);
  }
  return [...byDay.entries()]
    .map(([date, total]) => ({ date, total }))
    .sort((a, b) => a.date.localeCompare(b.date));
}

export function listTransactions(db: PrismaClient, range?: Range): Promise<TransactionWithItems[]> {
  return db.transaction.findMany({
    where: where(range),
    include: { items: true },
    orderBy: { createdAt: "desc" },
  });
}
```

- [ ] **Step 4: Jalankan test**

Run: `npx vitest run tests/reports.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/reports.ts tests/reports.test.ts
git commit -m "feat: report services (summary, daily, list)"
```

---

### Task 8: Auth (hash & verifikasi password) — TDD

**Files:**
- Create: `src/lib/auth.ts`
- Test: `tests/auth.test.ts`

**Interfaces:**
- Consumes: `prisma`, `bcryptjs`, `iron-session`.
- Produces:
  - `verifyCredentials(db, username: string, password: string): Promise<{ id: number; username: string } | null>`
  - `sessionOptions` (config iron-session) + `type SessionData = { userId?: number; username?: string }`.

- [ ] **Step 1: Tulis test gagal**

`tests/auth.test.ts`:
```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import type { PrismaClient } from "@prisma/client";
import bcrypt from "bcryptjs";
import { freshDb } from "./helpers/db";
import { verifyCredentials } from "@/lib/auth";

let db: PrismaClient;
beforeAll(async () => {
  db = freshDb("test-auth.db");
  await db.user.create({ data: { username: "admin", passwordHash: bcrypt.hashSync("admin123", 10) } });
});
afterAll(async () => { await db.$disconnect(); });

test("verifyCredentials menerima password benar", async () => {
  const u = await verifyCredentials(db, "admin", "admin123");
  expect(u?.username).toBe("admin");
});

test("verifyCredentials menolak password salah", async () => {
  expect(await verifyCredentials(db, "admin", "salah")).toBeNull();
});

test("verifyCredentials menolak user tidak ada", async () => {
  expect(await verifyCredentials(db, "nobody", "x")).toBeNull();
});
```

- [ ] **Step 2: Jalankan test**

Run: `npx vitest run tests/auth.test.ts`
Expected: FAIL ("Cannot find module '@/lib/auth'").

- [ ] **Step 3: Implementasi**

`src/lib/auth.ts`:
```ts
import type { PrismaClient } from "@prisma/client";
import bcrypt from "bcryptjs";
import type { SessionOptions } from "iron-session";

export type SessionData = { userId?: number; username?: string };

export const sessionOptions: SessionOptions = {
  password: process.env.SESSION_PASSWORD as string,
  cookieName: "pos_session",
  cookieOptions: { secure: process.env.NODE_ENV === "production" },
};

export async function verifyCredentials(db: PrismaClient, username: string, password: string) {
  const user = await db.user.findUnique({ where: { username } });
  if (!user) return null;
  if (!bcrypt.compareSync(password, user.passwordHash)) return null;
  return { id: user.id, username: user.username };
}
```

- [ ] **Step 4: Jalankan test**

Run: `npx vitest run tests/auth.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/auth.ts tests/auth.test.ts
git commit -m "feat: auth credential verification + session config"
```

---

### Task 9: Route Handlers (API) — produk, transaksi, laporan, login

**Files:**
- Create: `src/app/api/login/route.ts`, `src/app/api/logout/route.ts`
- Create: `src/app/api/products/route.ts`, `src/app/api/products/[id]/route.ts`
- Create: `src/app/api/transactions/route.ts`, `src/app/api/transactions/[id]/route.ts`
- Create: `src/app/api/reports/summary/route.ts`
- Create: `src/lib/session.ts` (helper baca sesi di Route Handler)

**Interfaces:**
- Consumes: semua service dari Task 5–8, `prisma`, `sessionOptions`.
- Produces: endpoint JSON yang dipakai UI di Task 11–14.
  - `POST /api/login {username,password}` → 200 `{ok:true}` / 401.
  - `POST /api/logout` → 200.
  - `GET /api/products?activeOnly=1` → `Product[]`; `POST /api/products` → `Product`.
  - `PATCH /api/products/:id`; `DELETE /api/products/:id`.
  - `GET /api/transactions` → `TransactionWithItems[]`; `POST /api/transactions {lines,paidAmount,paymentMethod}` → `TransactionWithItems`.
  - `GET /api/transactions/:id` → `TransactionWithItems`.
  - `GET /api/reports/summary?from=&to=` → `{ summary, daily }`.

- [ ] **Step 1: Helper sesi**

`src/lib/session.ts`:
```ts
import { cookies } from "next/headers";
import { getIronSession } from "iron-session";
import { sessionOptions, type SessionData } from "@/lib/auth";

export async function getSession() {
  return getIronSession<SessionData>(await cookies(), sessionOptions);
}
```

- [ ] **Step 2: Login & logout**

`src/app/api/login/route.ts`:
```ts
import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { verifyCredentials } from "@/lib/auth";
import { getSession } from "@/lib/session";

export async function POST(req: Request) {
  const { username, password } = await req.json();
  const user = await verifyCredentials(prisma, username ?? "", password ?? "");
  if (!user) return NextResponse.json({ error: "Login gagal" }, { status: 401 });
  const session = await getSession();
  session.userId = user.id;
  session.username = user.username;
  await session.save();
  return NextResponse.json({ ok: true });
}
```

`src/app/api/logout/route.ts`:
```ts
import { NextResponse } from "next/server";
import { getSession } from "@/lib/session";

export async function POST() {
  const session = await getSession();
  session.destroy();
  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 3: Produk**

`src/app/api/products/route.ts`:
```ts
import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { listProducts, createProduct } from "@/lib/products";

export async function GET(req: Request) {
  const activeOnly = new URL(req.url).searchParams.get("activeOnly") === "1";
  return NextResponse.json(await listProducts(prisma, { activeOnly }));
}

export async function POST(req: Request) {
  try {
    const body = await req.json();
    return NextResponse.json(await createProduct(prisma, body));
  } catch (e) {
    return NextResponse.json({ error: (e as Error).message }, { status: 400 });
  }
}
```

`src/app/api/products/[id]/route.ts`:
```ts
import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { updateProduct, deleteProduct } from "@/lib/products";

export async function PATCH(req: Request, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  try {
    return NextResponse.json(await updateProduct(prisma, Number(id), await req.json()));
  } catch (e) {
    return NextResponse.json({ error: (e as Error).message }, { status: 400 });
  }
}

export async function DELETE(_req: Request, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  await deleteProduct(prisma, Number(id));
  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 4: Transaksi**

`src/app/api/transactions/route.ts`:
```ts
import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { createTransaction } from "@/lib/transactions";
import { listTransactions } from "@/lib/reports";

export async function GET() {
  return NextResponse.json(await listTransactions(prisma));
}

export async function POST(req: Request) {
  try {
    return NextResponse.json(await createTransaction(prisma, await req.json()));
  } catch (e) {
    return NextResponse.json({ error: (e as Error).message }, { status: 400 });
  }
}
```

`src/app/api/transactions/[id]/route.ts`:
```ts
import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";

export async function GET(_req: Request, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const tx = await prisma.transaction.findUnique({
    where: { id: Number(id) },
    include: { items: { include: { product: true } } },
  });
  if (!tx) return NextResponse.json({ error: "Tidak ditemukan" }, { status: 404 });
  return NextResponse.json(tx);
}
```

- [ ] **Step 5: Laporan**

`src/app/api/reports/summary/route.ts`:
```ts
import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { getSummary, getDailySales } from "@/lib/reports";

export async function GET(req: Request) {
  const sp = new URL(req.url).searchParams;
  const from = sp.get("from") ? new Date(sp.get("from")!) : undefined;
  const to = sp.get("to") ? new Date(sp.get("to")!) : undefined;
  const range = { from, to };
  return NextResponse.json({
    summary: await getSummary(prisma, range),
    daily: await getDailySales(prisma, range),
  });
}
```

- [ ] **Step 6: Verifikasi build kompilasi**

Run: `npx tsc --noEmit`
Expected: tidak ada error tipe.

- [ ] **Step 7: Commit**

```bash
git add src/app/api src/lib/session.ts
git commit -m "feat: API route handlers (auth, products, transactions, reports)"
```

---

### Task 10: Proteksi rute via middleware

**Files:**
- Create: `src/middleware.ts`

**Interfaces:**
- Consumes: cookie sesi `pos_session`.
- Produces: redirect ke `/login` untuk semua halaman kecuali `/login` dan aset, bila belum login.

- [ ] **Step 1: Tulis middleware**

`src/middleware.ts`:
```ts
import { NextResponse, type NextRequest } from "next/server";
import { getIronSession } from "iron-session";
import { sessionOptions, type SessionData } from "@/lib/auth";

export async function middleware(req: NextRequest) {
  const res = NextResponse.next();
  const session = await getIronSession<SessionData>(req, res, sessionOptions);
  const isLogin = req.nextUrl.pathname.startsWith("/login");
  const isApiLogin = req.nextUrl.pathname.startsWith("/api/login");
  if (!session.userId && !isLogin && !isApiLogin) {
    return NextResponse.redirect(new URL("/login", req.url));
  }
  return res;
}

export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico).*)"],
};
```

- [ ] **Step 2: Verifikasi kompilasi**

Run: `npx tsc --noEmit`
Expected: tidak ada error.

- [ ] **Step 3: Commit**

```bash
git add src/middleware.ts
git commit -m "feat: route protection middleware"
```

---

### Task 11: Fondasi UI minimalist-ui (layout, token, navigasi)

> **Arah desain:** terapkan skill `minimalist-ui` (paket taste-skill). Install dulu via `/plugin` jika belum, lalu invoke skill itu untuk panduan warna/tipografi/spacing. Prinsip inti: monokrom hangat, bento-grid, flat (tanpa gradient/shadow berat), kontras tipografi kuat. Hindari tampilan "generic AI".

**Files:**
- Modify: `src/app/globals.css`, `src/app/layout.tsx`
- Create: `src/app/components/AppNav.tsx`
- Modify: `src/app/page.tsx` (redirect ke `/kasir`)

- [ ] **Step 1: Token warna minimalist-ui di `globals.css`**

Tambahkan setelah baris `@import "tailwindcss";`:
```css
:root {
  --bg: #faf9f7;
  --surface: #ffffff;
  --ink: #1a1a1a;
  --muted: #6b6b6b;
  --line: #e7e4df;
  --accent: #2f6f4f;
}
body { background: var(--bg); color: var(--ink); }
```

- [ ] **Step 2: Navigasi**

`src/app/components/AppNav.tsx`:
```tsx
import Link from "next/link";

export function AppNav() {
  const items = [
    { href: "/kasir", label: "Kasir" },
    { href: "/produk", label: "Produk" },
    { href: "/laporan", label: "Laporan" },
  ];
  return (
    <nav className="flex items-center gap-1 border-b border-[var(--line)] bg-[var(--surface)] px-6 py-3">
      <span className="mr-6 text-lg font-semibold tracking-tight">POS</span>
      {items.map((it) => (
        <Link key={it.href} href={it.href} className="rounded px-3 py-1.5 text-sm text-[var(--muted)] hover:bg-[var(--bg)] hover:text-[var(--ink)]">
          {it.label}
        </Link>
      ))}
      <form action="/api/logout" method="post" className="ml-auto">
        <button className="text-sm text-[var(--muted)] hover:text-[var(--ink)]">Keluar</button>
      </form>
    </nav>
  );
}
```

- [ ] **Step 3: Root layout pakai nav**

`src/app/layout.tsx` (ganti isi body):
```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = { title: "POS Kasir" };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  );
}
```

- [ ] **Step 4: Redirect home ke kasir**

`src/app/page.tsx`:
```tsx
import { redirect } from "next/navigation";
export default function Home() { redirect("/kasir"); }
```

- [ ] **Step 5: Jalankan dev & cek mata**

Run: `npm run dev` lalu buka `http://localhost:3000` → diarahkan ke `/login` (karena middleware).
Expected: tidak ada error kompilasi di terminal.

- [ ] **Step 6: Commit**

```bash
git add src/app/globals.css src/app/layout.tsx src/app/components/AppNav.tsx src/app/page.tsx
git commit -m "feat: minimalist-ui base layout + nav"
```

---

### Task 12: Halaman Login

**Files:**
- Create: `src/app/login/page.tsx`

- [ ] **Step 1: Form login (client component)**

`src/app/login/page.tsx`:
```tsx
"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";

export default function LoginPage() {
  const router = useRouter();
  const [username, setU] = useState("");
  const [password, setP] = useState("");
  const [err, setErr] = useState("");

  async function submit(e: React.FormEvent) {
    e.preventDefault();
    setErr("");
    const res = await fetch("/api/login", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ username, password }),
    });
    if (res.ok) router.push("/kasir");
    else setErr("Username atau password salah");
  }

  return (
    <main className="flex min-h-screen items-center justify-center px-4">
      <form onSubmit={submit} className="w-full max-w-sm rounded-lg border border-[var(--line)] bg-[var(--surface)] p-8">
        <h1 className="mb-6 text-xl font-semibold tracking-tight">Masuk POS</h1>
        <label className="mb-1 block text-sm text-[var(--muted)]">Username</label>
        <input value={username} onChange={(e) => setU(e.target.value)} className="mb-4 w-full rounded border border-[var(--line)] px-3 py-2" />
        <label className="mb-1 block text-sm text-[var(--muted)]">Password</label>
        <input type="password" value={password} onChange={(e) => setP(e.target.value)} className="mb-4 w-full rounded border border-[var(--line)] px-3 py-2" />
        {err && <p className="mb-4 text-sm text-red-600">{err}</p>}
        <button className="w-full rounded bg-[var(--accent)] py-2 text-white">Masuk</button>
      </form>
    </main>
  );
}
```

- [ ] **Step 2: Uji manual**

Run: `npm run dev`, buka `/login`, masuk dengan `admin` / `admin123`.
Expected: berhasil diarahkan ke `/kasir`.

- [ ] **Step 3: Commit**

```bash
git add src/app/login/page.tsx
git commit -m "feat: login page"
```

---

### Task 13: Halaman Produk (CRUD UI)

**Files:**
- Create: `src/app/produk/page.tsx`
- Create: `src/app/components/ProductForm.tsx`

**Interfaces:**
- Consumes: `GET/POST /api/products`, `PATCH/DELETE /api/products/:id`, `formatRupiah`.

- [ ] **Step 1: Form produk**

`src/app/components/ProductForm.tsx`:
```tsx
"use client";
import { useState } from "react";

export type ProductInput = { name: string; price: number; stock: number; sku?: string; category?: string };

export function ProductForm({ onSubmit }: { onSubmit: (p: ProductInput) => Promise<void> }) {
  const [name, setName] = useState("");
  const [price, setPrice] = useState("");
  const [stock, setStock] = useState("");

  async function submit(e: React.FormEvent) {
    e.preventDefault();
    await onSubmit({ name, price: Number(price) || 0, stock: Number(stock) || 0 });
    setName(""); setPrice(""); setStock("");
  }

  return (
    <form onSubmit={submit} className="flex flex-wrap gap-2 rounded-lg border border-[var(--line)] bg-[var(--surface)] p-4">
      <input placeholder="Nama" value={name} onChange={(e) => setName(e.target.value)} className="flex-1 rounded border border-[var(--line)] px-3 py-2" />
      <input placeholder="Harga" value={price} onChange={(e) => setPrice(e.target.value)} className="w-32 rounded border border-[var(--line)] px-3 py-2" />
      <input placeholder="Stok" value={stock} onChange={(e) => setStock(e.target.value)} className="w-24 rounded border border-[var(--line)] px-3 py-2" />
      <button className="rounded bg-[var(--accent)] px-4 py-2 text-white">Tambah</button>
    </form>
  );
}
```

- [ ] **Step 2: Halaman produk**

`src/app/produk/page.tsx`:
```tsx
"use client";
import { useEffect, useState } from "react";
import { AppNav } from "@/app/components/AppNav";
import { ProductForm, type ProductInput } from "@/app/components/ProductForm";
import { formatRupiah } from "@/lib/money";

type Product = { id: number; name: string; price: number; stock: number; category?: string | null };

export default function ProdukPage() {
  const [products, setProducts] = useState<Product[]>([]);

  async function load() {
    const res = await fetch("/api/products?activeOnly=1");
    setProducts(await res.json());
  }
  useEffect(() => { load(); }, []);

  async function add(p: ProductInput) {
    await fetch("/api/products", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(p) });
    await load();
  }
  async function remove(id: number) {
    await fetch(`/api/products/${id}`, { method: "DELETE" });
    await load();
  }

  return (
    <>
      <AppNav />
      <main className="mx-auto max-w-4xl p-6">
        <h1 className="mb-4 text-2xl font-semibold tracking-tight">Produk</h1>
        <div className="mb-6"><ProductForm onSubmit={add} /></div>
        <div className="overflow-hidden rounded-lg border border-[var(--line)] bg-[var(--surface)]">
          <table className="w-full text-sm">
            <thead className="border-b border-[var(--line)] text-left text-[var(--muted)]">
              <tr><th className="p-3">Nama</th><th className="p-3">Harga</th><th className="p-3">Stok</th><th className="p-3"></th></tr>
            </thead>
            <tbody>
              {products.map((p) => (
                <tr key={p.id} className="border-b border-[var(--line)] last:border-0">
                  <td className="p-3">{p.name}</td>
                  <td className="p-3">{formatRupiah(p.price)}</td>
                  <td className="p-3">{p.stock}</td>
                  <td className="p-3 text-right"><button onClick={() => remove(p.id)} className="text-red-600 hover:underline">Hapus</button></td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </main>
    </>
  );
}
```

- [ ] **Step 3: Uji manual**

Run: `npm run dev`, buka `/produk`. Tambah produk, lihat muncul di tabel, hapus.
Expected: berfungsi tanpa error konsol.

- [ ] **Step 4: Commit**

```bash
git add src/app/produk/page.tsx src/app/components/ProductForm.tsx
git commit -m "feat: product management page"
```

---

### Task 14: Halaman Kasir + cetak struk

**Files:**
- Create: `src/app/kasir/page.tsx`
- Create: `src/app/components/Receipt.tsx`

**Interfaces:**
- Consumes: `GET /api/products?activeOnly=1`, `POST /api/transactions`, `calcTotal`, `calcChange`, `formatRupiah`, `CartLine`.

- [ ] **Step 1: Komponen struk (80mm, print)**

`src/app/components/Receipt.tsx`:
```tsx
import { formatRupiah } from "@/lib/money";
import type { CartLine } from "@/lib/cart";

export function Receipt({ lines, total, paid, change, invoiceNo }: {
  lines: CartLine[]; total: number; paid: number; change: number; invoiceNo: string;
}) {
  return (
    <div id="receipt" className="hidden print:block" style={{ width: "80mm", padding: "4mm", fontSize: "12px", fontFamily: "monospace" }}>
      <div style={{ textAlign: "center", fontWeight: 700 }}>TOKO SAYA</div>
      <div style={{ textAlign: "center" }}>{new Date().toLocaleString("id-ID")}</div>
      <div>{invoiceNo}</div>
      <hr />
      {lines.map((l) => (
        <div key={l.productId} style={{ display: "flex", justifyContent: "space-between" }}>
          <span>{l.name} x{l.quantity}</span><span>{formatRupiah(l.price * l.quantity)}</span>
        </div>
      ))}
      <hr />
      <div style={{ display: "flex", justifyContent: "space-between" }}><span>Total</span><span>{formatRupiah(total)}</span></div>
      <div style={{ display: "flex", justifyContent: "space-between" }}><span>Bayar</span><span>{formatRupiah(paid)}</span></div>
      <div style={{ display: "flex", justifyContent: "space-between" }}><span>Kembali</span><span>{formatRupiah(change)}</span></div>
      <div style={{ textAlign: "center", marginTop: "8px" }}>Terima kasih</div>
    </div>
  );
}
```

> Catatan: pada `globals.css` tambahkan aturan print agar hanya struk yang tercetak:
> ```css
> @media print { body * { visibility: hidden; } #receipt, #receipt * { visibility: visible; } #receipt { position: absolute; left: 0; top: 0; } }
> ```

- [ ] **Step 2: Tambah aturan print ke `globals.css`**

```css
@media print {
  body * { visibility: hidden; }
  #receipt, #receipt * { visibility: visible; }
  #receipt { position: absolute; left: 0; top: 0; }
}
.print\:block { }
```

- [ ] **Step 3: Halaman kasir**

`src/app/kasir/page.tsx`:
```tsx
"use client";
import { useEffect, useState } from "react";
import { AppNav } from "@/app/components/AppNav";
import { Receipt } from "@/app/components/Receipt";
import { calcTotal, calcChange, type CartLine } from "@/lib/cart";
import { formatRupiah, parseRupiah } from "@/lib/money";

type Product = { id: number; name: string; price: number; stock: number };

export default function KasirPage() {
  const [products, setProducts] = useState<Product[]>([]);
  const [lines, setLines] = useState<CartLine[]>([]);
  const [paidText, setPaidText] = useState("");
  const [lastInvoice, setLastInvoice] = useState("");

  useEffect(() => {
    fetch("/api/products?activeOnly=1").then((r) => r.json()).then(setProducts);
  }, []);

  function addToCart(p: Product) {
    setLines((prev) => {
      const found = prev.find((l) => l.productId === p.id);
      if (found) return prev.map((l) => (l.productId === p.id ? { ...l, quantity: l.quantity + 1 } : l));
      return [...prev, { productId: p.id, name: p.name, price: p.price, quantity: 1 }];
    });
  }

  const total = calcTotal(lines);
  const paid = parseRupiah(paidText);
  let change = 0;
  try { change = calcChange(total, paid); } catch { change = 0; }

  async function pay() {
    if (lines.length === 0) return;
    const res = await fetch("/api/transactions", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ lines: lines.map((l) => ({ productId: l.productId, quantity: l.quantity })), paidAmount: paid }),
    });
    if (!res.ok) { alert((await res.json()).error); return; }
    const tx = await res.json();
    setLastInvoice(tx.invoiceNo);
    setTimeout(() => window.print(), 50);
    setLines([]); setPaidText("");
    fetch("/api/products?activeOnly=1").then((r) => r.json()).then(setProducts);
  }

  return (
    <>
      <AppNav />
      <main className="mx-auto grid max-w-6xl grid-cols-1 gap-6 p-6 md:grid-cols-[1fr_360px]">
        <section>
          <h1 className="mb-4 text-2xl font-semibold tracking-tight">Kasir</h1>
          <div className="grid grid-cols-2 gap-3 sm:grid-cols-3">
            {products.map((p) => (
              <button key={p.id} onClick={() => addToCart(p)} disabled={p.stock <= 0}
                className="rounded-lg border border-[var(--line)] bg-[var(--surface)] p-4 text-left hover:border-[var(--accent)] disabled:opacity-40">
                <div className="font-medium">{p.name}</div>
                <div className="text-sm text-[var(--muted)]">{formatRupiah(p.price)}</div>
                <div className="text-xs text-[var(--muted)]">Stok: {p.stock}</div>
              </button>
            ))}
          </div>
        </section>

        <aside className="rounded-lg border border-[var(--line)] bg-[var(--surface)] p-4">
          <h2 className="mb-3 font-semibold">Keranjang</h2>
          {lines.length === 0 && <p className="text-sm text-[var(--muted)]">Belum ada item</p>}
          {lines.map((l) => (
            <div key={l.productId} className="flex justify-between py-1 text-sm">
              <span>{l.name} x{l.quantity}</span><span>{formatRupiah(l.price * l.quantity)}</span>
            </div>
          ))}
          <div className="mt-3 flex justify-between border-t border-[var(--line)] pt-3 font-semibold">
            <span>Total</span><span>{formatRupiah(total)}</span>
          </div>
          <label className="mt-3 block text-sm text-[var(--muted)]">Bayar</label>
          <input value={paidText} onChange={(e) => setPaidText(e.target.value)} className="w-full rounded border border-[var(--line)] px-3 py-2" />
          <div className="mt-2 flex justify-between text-sm">
            <span>Kembali</span><span>{formatRupiah(change)}</span>
          </div>
          <button onClick={pay} disabled={lines.length === 0 || paid < total}
            className="mt-4 w-full rounded bg-[var(--accent)] py-2 text-white disabled:opacity-40">
            Bayar & Cetak
          </button>
        </aside>
      </main>
      <Receipt lines={lines} total={total} paid={paid} change={change} invoiceNo={lastInvoice} />
    </>
  );
}
```

> Catatan implementer: karena `setLines([])` dipanggil sebelum print bisa mengosongkan struk, simpan snapshot baris terakhir di state terpisah (mis. `printData`) sebelum reset, lalu render `<Receipt>` dari snapshot itu. Lakukan penyesuaian kecil ini saat Step 4 bila struk tercetak kosong.

- [ ] **Step 4: Uji manual end-to-end**

Run: `npm run dev`. Di `/kasir`: klik produk → masuk keranjang, total otomatis; isi bayar `50000` → kembalian otomatis; klik Bayar & Cetak → dialog print muncul berisi struk; stok produk berkurang setelah refresh.
Expected: semua berfungsi; jika struk kosong, terapkan catatan snapshot di Step 3.

- [ ] **Step 5: Commit**

```bash
git add src/app/kasir/page.tsx src/app/components/Receipt.tsx src/app/globals.css
git commit -m "feat: cashier page with auto-calc and thermal receipt print"
```

---

### Task 15: Halaman Laporan

**Files:**
- Create: `src/app/laporan/page.tsx`

**Interfaces:**
- Consumes: `GET /api/reports/summary?from=&to=`, `GET /api/transactions`, `formatRupiah`, Recharts.

- [ ] **Step 1: Halaman laporan**

`src/app/laporan/page.tsx`:
```tsx
"use client";
import { useEffect, useState } from "react";
import { AppNav } from "@/app/components/AppNav";
import { formatRupiah } from "@/lib/money";
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer } from "recharts";

type Summary = { totalSales: number; transactionCount: number };
type Daily = { date: string; total: number };
type Tx = { id: number; invoiceNo: string; createdAt: string; totalAmount: number };

export default function LaporanPage() {
  const [summary, setSummary] = useState<Summary>({ totalSales: 0, transactionCount: 0 });
  const [daily, setDaily] = useState<Daily[]>([]);
  const [txs, setTxs] = useState<Tx[]>([]);

  useEffect(() => {
    fetch("/api/reports/summary").then((r) => r.json()).then((d) => { setSummary(d.summary); setDaily(d.daily); });
    fetch("/api/transactions").then((r) => r.json()).then(setTxs);
  }, []);

  return (
    <>
      <AppNav />
      <main className="mx-auto max-w-5xl p-6">
        <h1 className="mb-4 text-2xl font-semibold tracking-tight">Laporan</h1>
        <div className="mb-6 grid grid-cols-2 gap-4">
          <div className="rounded-lg border border-[var(--line)] bg-[var(--surface)] p-5">
            <div className="text-sm text-[var(--muted)]">Total Penjualan</div>
            <div className="mt-1 text-2xl font-semibold">{formatRupiah(summary.totalSales)}</div>
          </div>
          <div className="rounded-lg border border-[var(--line)] bg-[var(--surface)] p-5">
            <div className="text-sm text-[var(--muted)]">Jumlah Transaksi</div>
            <div className="mt-1 text-2xl font-semibold">{summary.transactionCount}</div>
          </div>
        </div>
        <div className="mb-6 h-64 rounded-lg border border-[var(--line)] bg-[var(--surface)] p-4">
          <ResponsiveContainer width="100%" height="100%">
            <BarChart data={daily}>
              <XAxis dataKey="date" fontSize={12} />
              <YAxis fontSize={12} />
              <Tooltip formatter={(v) => formatRupiah(Number(v))} />
              <Bar dataKey="total" fill="#2f6f4f" />
            </BarChart>
          </ResponsiveContainer>
        </div>
        <div className="overflow-hidden rounded-lg border border-[var(--line)] bg-[var(--surface)]">
          <table className="w-full text-sm">
            <thead className="border-b border-[var(--line)] text-left text-[var(--muted)]">
              <tr><th className="p-3">Invoice</th><th className="p-3">Tanggal</th><th className="p-3">Total</th></tr>
            </thead>
            <tbody>
              {txs.map((t) => (
                <tr key={t.id} className="border-b border-[var(--line)] last:border-0">
                  <td className="p-3">{t.invoiceNo}</td>
                  <td className="p-3">{new Date(t.createdAt).toLocaleString("id-ID")}</td>
                  <td className="p-3">{formatRupiah(t.totalAmount)}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </main>
    </>
  );
}
```

- [ ] **Step 2: Uji manual**

Run: `npm run dev`, buka `/laporan` setelah membuat beberapa transaksi.
Expected: kartu ringkasan terisi, grafik tampil, daftar transaksi muncul.

- [ ] **Step 3: Commit**

```bash
git add src/app/laporan/page.tsx
git commit -m "feat: sales report page with chart"
```

---

### Task 16: Verifikasi akhir & dokumentasi

**Files:**
- Create: `README.md`

- [ ] **Step 1: Jalankan seluruh test**

Run: `npm test`
Expected: semua test (money, cart, products, transactions, reports, auth, smoke) PASS.

- [ ] **Step 2: Build produksi**

Run: `npm run build`
Expected: build sukses tanpa error.

- [ ] **Step 3: Tulis `README.md`**

```markdown
# POS Kasir

Aplikasi POS satu-toko: manajemen produk, transaksi (hitung otomatis), cetak struk thermal, laporan.

## Setup
1. `npm install`
2. `npx prisma migrate dev`
3. `npx prisma db seed`  (login: admin / admin123)
4. `npm run dev`

## Test
`npm test`

## Stack
Next.js, TypeScript, Tailwind, Prisma, SQLite, Recharts.
```

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: add README"
```

---

## Self-Review Notes

- **Spec coverage:** Manajemen produk → Task 5,13. Transaksi + hitung otomatis → Task 4,6,14. Cetak struk thermal (cara A) → Task 14. Laporan → Task 7,15. Login sederhana → Task 8,9,10,12. SQLite/Prisma → Task 2. Minimalist-ui → Task 11. Semua tercakup.
- **Integritas transaksi & priceAtSale** → Task 6 (diuji).
- **Catatan implementer di Task 14 Step 3** sengaja diberikan karena reset state sebelum `window.print()` adalah jebakan umum; perbaikan snapshot dijelaskan konkret.
